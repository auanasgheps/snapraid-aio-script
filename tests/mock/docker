#!/usr/bin/env bash
# Mock docker binary for tests.
# Appends all arguments to $MOCK_DOCKER_LOG if configured.

if [ -n "${MOCK_DOCKER_LOG:-}" ] && [ "$MOCK_DOCKER_LOG" != "/docker_mock.log" ]; then
  echo "$*" >>"$MOCK_DOCKER_LOG"
fi

exit 0
